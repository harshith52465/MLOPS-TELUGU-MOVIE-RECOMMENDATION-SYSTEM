version: "3.9"

services:
  backend:
    build: ./backend
    container_name: ml-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: ml-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
